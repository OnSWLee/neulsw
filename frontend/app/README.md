# app 폴더 안내

이 폴더는 이전 Next.js 스타일 참고용으로 남겨둔 것입니다.

- **실제 앱은 Vite + React**로 동작하며, 진입점은 `src/main.tsx`, 라우팅은 `src/App.tsx`에서 처리합니다.
- **블로그 상세 페이지**는 `src/pages/BlogPost.tsx`에서 처리하며, 경로는 `/blog/:slug` 입니다.

Next.js를 사용하지 않으므로 이 폴더의 코드는 빌드/실행에 포함되지 않습니다. 필요 시 참고만 하시고, 정리가 필요하면 이 폴더를 삭제해도 됩니다.
